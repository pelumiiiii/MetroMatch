# Minimal Requirements for MetroMatch
# Install with: pip install -r requirements-minimal.txt
# This installs only the essential packages needed to run the core features

# Database
pymongo>=4.0.0
dnspython>=2.0.0

# HTTP Requests & Web Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
playwright>=1.40.0  # Browser automation for UI search fallback

# Environment Management
python-dotenv>=1.0.0

# Metronome (choose one - pygame recommended)
pygame>=2.5.0
numpy>=1.24.0  # Required for audio generation
